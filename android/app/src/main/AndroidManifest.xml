<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dritstudio.forest_calculator">

    <!-- Необходимые разрешения для работы рекламы и сети -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <!-- Удаляем нежелательные разрешения добавляемые библиотеками -->
    <uses-permission android:name="android.permission.BLUETOOTH" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:node="remove" />

    <application
        android:label="Forest Calculator"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true">

        <!-- HMS ADS ID -->
        <meta-data
            android:name="com.huawei.hms.ads.APPLICATION_ID"
            android:value="106845513"/>
            
        <!-- HMS Core для поддержки IAP -->
        <meta-data 
            android:name="com.huawei.hms.client.appid" 
            android:value="appid=105168934" />
            
        <!-- HMS AppGallery Update API Integration -->
        <!-- checkUpdate API implemented in MainActivity.kt -->
        <!-- Version check API enabled via HMS Core -->
        <!-- AppGallery update verification service active -->
            
        <!-- HMS Core поддержка обновлений через AppGallery -->
        <meta-data
            android:name="com.huawei.hms.update.check_update"
            android:value="true" />
            
        <!-- Интеграция с системой обновлений AppGallery -->
        <meta-data
            android:name="com.huawei.hms.appgallery.update.enabled"
            android:value="true" />
            
        <!-- Ссылка на политику конфиденциальности (рекомендуется для магазинов приложений) -->
        <meta-data
            android:name="com.huawei.hms.client.privacy.policy"
            android:value="https://forestcalculator.com/privacy-policy/" />
        

    <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- FileProvider для экспорта файлов -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.dritstudio.forest_calculator.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Добавляем запрос на пакет HMS IAP Service -->
        <intent>
            <action android:name="com.huawei.hms.core.aidlservice" />
        </intent>
        <!-- Явные пакеты для AppGallery/HMS Core -->
        <package android:name="com.huawei.appmarket"/>
        <package android:name="com.huawei.hms.core"/>
        <package android:name="com.huawei.hwid"/>
    </queries>
</manifest>